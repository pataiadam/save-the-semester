<div class="row">
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title">&nbsp;Learners</h3>
    </div>
    <div class="panel-body">
      <div class="table-responsive">
        <table class="table table-bordered table-hover table-striped">
          <thead>
            <tr>
              <th>id</th>
              <th>user</th>
              <th>coach</th>
              <th>actions</th>
            </tr>
          </thead>
          <tbody>
            {{#each learners}}
              <tr>
                <td>{{this.id}}</td>
                <td>{{this.userId}}</td>
                <td>{{this.coachId}}</td>
                <td>
                  <a href="/learner/show?id={{this.id}}" class="btn btn-xs btn-primary" role="button">Show</a>
                  <a href="/learner/update?id={{this.id}}" class="btn btn-xs btn-primary" role="button">Update</a>
                  {{#unless this.deletedAt}}
                    <a href="/learner/delete?id={{this.id}}" class="btn btn-xs btn-danger" role="button">Delete</a>
                  {{/unless}}
                </td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
      <div class="text-right">
        <a href="/learner/create">Create new learner <i class="fa fa-arrow-circle-right"></i></a>
      </div>
    </div>
  </div>
</div>
